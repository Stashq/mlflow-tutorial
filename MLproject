name: tutorial

entry_points:
  main:
    parameters:
      # data_file: path
      alpha: {type: float, default: 0.1}
      l1_ratio: {type: float, default: 0.1}
    command: "python examples/2.py {alpha} {l1_ratio}"
    # command: "python train.py -r {regularization} {data_file}"
